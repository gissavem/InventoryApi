version: "3.8"

services: 
  ingredient-api:
    container_name: ingredient-api
    build: 
      context: ./ingredient-api/
    image: ingredient-api:latest
    ports: 
      - "8080:80"
    networks: 
      - ingredient-api
    volumes: 
      - "./database:/app/database"

  frontend:
    container_name: frontend
    build: 
      context: ./frontend/
    image: frontend:latest
    ports: 
      - "4200:80"
    networks: 
      - ingredient-api

networks: 
  ingredient-api:
    external: false